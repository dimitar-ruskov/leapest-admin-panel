import { TableGridDirective } from './table-grid.directive';

describe('TableGridDirective', () => {
  it('should create an instance', () => {
    const directive = new TableGridDirective();
    expect(directive).toBeTruthy();
  });
});

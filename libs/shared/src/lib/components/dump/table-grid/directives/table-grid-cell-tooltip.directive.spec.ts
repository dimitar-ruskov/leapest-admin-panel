import { TableGridCellTooltipDirective } from './table-grid-cell-tooltip.directive';

describe('TableGridCellTooltipDirective', () => {
  it('should create an instance', () => {
    const directive = new TableGridCellTooltipDirective();
    expect(directive).toBeTruthy();
  });
});

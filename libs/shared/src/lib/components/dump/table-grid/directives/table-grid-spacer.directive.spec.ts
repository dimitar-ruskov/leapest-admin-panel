import { TableGridSpacerDirective } from './table-grid-spacer.directive';

describe('TableGridSpacerDirective', () => {
  it('should create an instance', () => {
    const directive = new TableGridSpacerDirective();
    expect(directive).toBeTruthy();
  });
});

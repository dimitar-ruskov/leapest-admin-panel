import { TableGridRowDirective } from './table-grid-row.directive';

describe('TableGridRowDirective', () => {
  it('should create an instance', () => {
    const directive = new TableGridRowDirective();
    expect(directive).toBeTruthy();
  });
});
